"""Core Scale-MS exceptions."""

class ScaleMSError(BaseException):
    """Base exception for scalems package errors.

    Users should be able to use this base class to catch errors 
    emitted by SCALE-MS.
    """